#include <bits/stdc++.h>
using namespace std;
int64_t arr[100000];

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t;
    cin >> t;
    while(t--){
        int n;
        cin >> n;
        cout << n << '\n';
    }
    return 0;
}
