#include <bits/stdc++.h>
using namespace std;

int main(){
    int n,k;
    scanf("%d %d",&n,&k);
    int value[n], numCases[k+1] = {1,0,};
    for (int i=0;i<n;i++) {
        scanf("%d", &value[i]);
    }

    return 0;
}